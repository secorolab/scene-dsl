{%- macro identifier(name) -%}
{{ name | replace('/', '_') | replace('-', '_') }}
{%- endmacro -%}

{%- macro vector(values) -%}
KDL::Vector({{ values[0] }}, {{ values[1] }}, {{ values[2] }})
{%- endmacro -%}

{%- macro joint(joint) -%}
{%- if joint is none -%}
KDL::Joint(KDL::Joint::None)
{%- elif joint.kind == 'fixed' -%}
KDL::Joint({% if joint.name %}"{{ joint.name }}", {% endif %}KDL::Joint::None)
{%- elif joint.kind == 'revolute' -%}
KDL::Joint("{{ joint.name }}", {{ vector(joint.origin) }}, {{ vector(joint.axis) }}, KDL::Joint::RotAxis)
{%- else -%}
#error "no KDL joint for a {{ joint.kind }} joint"
{%- endif -%}
{%- endmacro -%}

{%- macro inertia(inertia) -%}
{%- if inertia is none -%}
KDL::RigidBodyInertia::Zero()
{%- else -%}
KDL::RigidBodyInertia({{ inertia.mass }}, {{ vector(inertia.cog) }},
            KDL::RotationalInertia({{ inertia.moments[0] }}, {{ inertia.moments[1] }}, {{ inertia.moments[2] }}, {{ inertia.moments[3] }}, {{ inertia.moments[4] }}, {{ inertia.moments[5] }}))
{%- endif -%}
{%- endmacro -%}

{%- macro segment(segment) -%}
{%- set q = segment.transform.rotation.as_quat() -%}
KDL::Segment("{{ segment.name }}",
            {{ joint(segment.joint) }},
            KDL::Frame(KDL::Rotation::Quaternion({{ q[0] }}, {{ q[1] }}, {{ q[2] }}, {{ q[3] }}), {{ vector(segment.transform.translation) }}),
            {{ inertia(segment.inertia) }})
{%- endmacro -%}
// This is an auto-generated file. Do not edit it directly.
//
// KDL trees and chains for the kinematics of: {{ data.source }}
#pragma once

#include <kdl/chain.hpp>
#include <kdl/frames.hpp>
#include <kdl/tree.hpp>

namespace {{ identifier(data.name) }} {
{% for tree in data.trees %}
inline bool make_tree_{{ identifier(tree.name) }}(KDL::Tree *tree)
{
    *tree = KDL::Tree("{{ tree.root }}");
{%- for seg in tree.segments %}
    if (!tree->addSegment(
          {{ segment(seg) }},
          "{{ seg.hook }}")) {
        return false;  // no segment "{{ seg.hook }}" to attach "{{ seg.name }}" to
    }
{%- endfor %}
    return true;
}
{% for chain in tree.chains %}
inline bool make_chain_{{ identifier(chain.name) }}(KDL::Chain *chain)
{
    KDL::Tree tree;
    if (!make_tree_{{ identifier(tree.name) }}(&tree)) {
        return false;
    }
    // '{{ chain.root }}' -> '{{ chain.tip }}'
    return tree.getChain("{{ chain.root }}", "{{ chain.tip }}", *chain);
}
{% endfor %}
{%- endfor %}
}  // namespace {{ identifier(data.name) }}
